# written by HYDROLIB-core 1.0.1

[General]
fileVersion = 2.00     # File version. Do not edit this.
fileType    = iniField # File type. Should be 'iniField'. Do not edit this.

[Initial]
quantity            = waterlevel         # Name of the quantity. See UM Table D.2.
dataFile            = iniwaterlevels.asc # Name of file containing field data values.
dataFileType        = arcinfo            # Type of dataFile.
interpolationMethod = triangulation      # Type of (spatial) interpolation.
operand             = O                  # How this data is combined with previous data for the same quantity (if any).
extrapolationMethod = 0                  # Option for (spatial) extrapolation.
locationType        = 2d                 # Target location of interpolation.

[Initial]
quantity            = bedlevel        # Name of the quantity. See UM Table D.2.
dataFile            = inibedlevel.ini # Name of file containing field data values.
dataFileType        = 1dField         # Type of dataFile.
operand             = O               # How this data is combined with previous data for the same quantity (if any).
extrapolationMethod = 0               # Option for (spatial) extrapolation.
locationType        = all             # Target location of interpolation.

[Parameter]
quantity            = frictioncoefficient # Name of the quantity. See UM Table D.2.
dataFile            = manning.xyz         # Name of file containing field data values.
dataFileType        = sample              # Type of dataFile.
interpolationMethod = triangulation       # Type of (spatial) interpolation.
operand             = O                   # How this data is combined with previous data for the same quantity (if any).
extrapolationMethod = 0                   # Option for (spatial) extrapolation.
locationType        = all                 # Target location of interpolation.

[Parameter]
quantity            = frictioncoefficient # Name of the quantity. See UM Table D.2.
dataFile            = calibration1.pol    # Name of file containing field data values.
dataFileType        = polygon             # Type of dataFile.
interpolationMethod = constant            # Type of (spatial) interpolation.
operand             = *                   # How this data is combined with previous data for the same quantity (if any).
extrapolationMethod = 0                   # Option for (spatial) extrapolation.
locationType        = all                 # Target location of interpolation.
value               = 0.03                # Only for dataFileType=polygon. The constant value to be set inside for all model points inside the polygon.

